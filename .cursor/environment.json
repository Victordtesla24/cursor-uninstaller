{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "user": "node",
  "install": ".cursor/install.sh",
  "terminals": [
    {
      "name": "Dashboard",
      "command": "cd ui/dashboard && npx vite optimize --force && npm run dev -- --host --no-open",
      "description": "Runs the main dashboard application using Vite dev server (with pre-optimization), accessible externally and without opening browser."
    }
  ],
  "start": "./install.sh"
}
