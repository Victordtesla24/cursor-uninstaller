{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "user": "node",
  "install": ".cursor/install.sh",
  "terminals": [
    {
      "name": "Dashboard",
      "command": "cd ui/dashboard && npm run dashboard",
      "description": "Runs the main dashboard application from its subdirectory."
    }
  ]
}
