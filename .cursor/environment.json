{
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "user": "node",
  "install": ".cursor/install.sh",
  "terminals": [
    {
      "name": "Dashboard",
      "command": "cd ui/dashboard && npm run dev",
      "description": "Runs the main dashboard application using Vite dev server."
    }
  ]
}
